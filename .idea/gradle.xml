<?xml version="1.0" encoding="UTF-8"?>
<!--
  Tracked on purpose (see .gitignore). This pins two things IntelliJ otherwise
  decides on its own, both of which have broken this build before:

    gradleJvm         - ForgeGradle 2.3 only runs on Java 8. Without this, IntelliJ
                        uses the project SDK or its own bundled JDK (17/21), and the
                        build fails inside the plugin with an unrelated-looking error.
    distributionType  - DEFAULT_WRAPPED means "use the version in
                        gradle-wrapper.properties". This is what stops the IDE from
                        substituting or silently upgrading the Gradle distribution.

  The JDK name matches the ~/.jdks layout documented in dev-configurations.
-->
<project version="4">
  <component name="GradleMigrationSettings" migrationVersion="1" />
  <component name="GradleSettings">
    <option name="linkedExternalProjectsSettings">
      <GradleProjectSettings>
        <option name="distributionType" value="DEFAULT_WRAPPED" />
        <option name="externalProjectPath" value="$PROJECT_DIR$" />
        <option name="gradleJvm" value="azul-1.8.0_482" />
        <option name="modules">
          <set>
            <option value="$PROJECT_DIR$" />
          </set>
        </option>
        <option name="resolveExternalAnnotations" value="true" />
      </GradleProjectSettings>
    </option>
  </component>
</project>
